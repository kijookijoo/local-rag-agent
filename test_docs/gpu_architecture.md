# GPU Parallel Computing

Modern GPUs execute thousands of lightweight threads simultaneously.

Important concepts:
- CUDA cores
- memory bandwidth
- tensor operations
- parallel matrix multiplication

GPU acceleration is heavily used in machine learning workloads.